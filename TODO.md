# TODO

- [ ] Inspect current calendar/controls logic in `app.js`.
- [x] Add `state.interval` persistence to localStorage.

- [x] Populate `month`, `day`, `year` selects on stamp page.

- [x] Wire month/day/year selects to compute `stampedDateISO` and refresh calendar selection + stamp.

- [x] Wire `interval` select to persist and display in stamp note + final summary.

- [x] Update calendar grid to render for selected month/year (from selects/state), not only “now”.

- [x] Ensure clicking a day in the calendar updates selects, `state.stampedDateISO`, and stamp.

- [x] Update reset/restart behavior to clear interval/select UI.

- [ ] Quick manual test: pick date+interval, reload, continue, reset.




